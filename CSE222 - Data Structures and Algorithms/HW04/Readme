I have added also a new feature to file system that is saving to txt file to make it easy to work.
I also importing from it at the begining. The default file name is 'filesystem.txt' but you can change it in the code.
